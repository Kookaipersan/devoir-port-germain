<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de Bord - Port de Plaisance Russell</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <%- include('components/header') %>

    <div class="container mt-5">
        <h1>Bienvenue, <%= user.username %> !</h1>
        <p><strong>Email :</strong> <%= user.email %></p>
        <p><strong>Date du jour :</strong> <%= currentDate.toLocaleDateString('fr-FR') %></p>

        <h3 class="mt-4">Réservations en cours</h3>

        <% if (reservations.length > 0) { %>
            <table class="table table-bordered table-striped">
                <thead class="table-dark">
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Catway</th>
                        <th scope="col">Date de début</th>
                        <th scope="col">Date de fin</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% reservations.forEach(function(reservation) { %>
                        <tr>
                            <td><%= reservation._id %></td>
                            <td><%= reservation.catwayNumber %></td>
                            <td><%= new Date(reservation.startDate).toLocaleDateString('fr-FR') %></td>
                            <td><%= new Date(reservation.endDate).toLocaleDateString('fr-FR') %></td>
                            <td>
                                <a href="/reservations/<%= reservation._id %>" class="btn btn-info btn-sm">Voir</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-muted">Aucune réservation en cours.</p>
        <% } %>

        <div class="mt-4">
            <a href="/catways" class="btn btn-outline-primary me-2">Gérer les catways</a>
            <a href="/reservations" class="btn btn-outline-secondary me-2">Gérer les réservations</a>
            <a href="/users" class="btn btn-outline-success me-2">Gérer les utilisateurs</a>
            <a href="/documentation" class="btn btn-outline-info me-2">Documentation API</a>
            <a href="/login/logout" class="btn btn-danger">Se déconnecter</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
