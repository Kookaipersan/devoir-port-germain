<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body>
    <%- include('components/header') %>

    <div class="container mt-5">
        <h1 class="mb-4">Tableau de bord</h1>
        <p>Bienvenue, <%= user.username %> (Email : <%= user.email %>)</p>
        <p><strong>Date du jour :</strong> <%= new Date().toLocaleDateString() %></p>

        <!-- Menu de navigation -->
        <nav class="mt-4">
            <ul class="list-group">
                <li class="list-group-item"><a href="/catways">Gestion des Catways</a></li>
                <li class="list-group-item"><a href="/reservations">Gestion des Réservations</a></li>
                <li class="list-group-item"><a href="/users">Gestion des Utilisateurs</a></li>
                <li class="list-group-item"><a href="/logout" class="text-danger">Se déconnecter</a></li>
            </ul>
        </nav>

        <!-- Tableau des réservations en cours -->
        <h3 class="mt-5">Réservations en cours :</h3>
        
        <% if (reservations.length === 0) { %>
            <p>Aucune réservation en cours.</p>
        <% } else { %>
            <table class="table table-bordered mt-3">
                <thead>
                    <tr>
                        <th scope="col">Catway</th>
                        <th scope="col">Bateau</th>
                        <th scope="col">Client</th>
                        <th scope="col">Date de début</th>
                        <th scope="col">Date de fin</th>
                    </tr>
                </thead>
                <tbody>
                    <% reservations.forEach(function(reservation) { %>
                        <tr>
                            <td><%= reservation.catwayNumber %></td>
                            <td><%= reservation.boatName %></td>
                            <td><%= reservation.clientName %></td>
                            <td><%= reservation.startDate %></td>
                            <td><%= reservation.endDate %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>

    <!-- Bootstrap JS et Popper.js (si nécessaire pour certains composants interactifs) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>

</html>

